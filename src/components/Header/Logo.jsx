import {Link} from "react-router-dom";

const Logo = () => {
    return (
        <Link to="/" className="logo">
            <h2>Where in the world</h2>
        </Link>
    );
};

export default Logo;
